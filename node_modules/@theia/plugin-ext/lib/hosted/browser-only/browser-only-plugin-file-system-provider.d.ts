import { Disposable, Event, Emitter, URI } from '@theia/core';
import { FileChange, FileDeleteOptions, FileOverwriteOptions, FileSystemProvider, FileType, FileWriteOptions, Stat, WatchOptions } from '@theia/filesystem/lib/common/files';
import { FileService, FileServiceContribution } from '@theia/filesystem/lib/browser/file-service';
/**
 * Reads the plugin assets the build dropped under `PLUGINS_BASE_PATH`. With a backend those come
 * off its disk via `file:`; browser-only has to fetch them over HTTP.
 *
 * Read-only, and only for what goes through the `FileService`: color themes, icon themes, icon
 * fonts.
 */
export declare class BrowserOnlyPluginFileSystemProvider implements FileSystemProvider {
    readonly capabilities: number;
    readonly onDidChangeCapabilities: Event<any>;
    readonly onFileWatchError: Event<any>;
    protected readonly onDidChangeFileEmitter: Emitter<readonly FileChange[]>;
    readonly onDidChangeFile: Event<readonly FileChange[]>;
    watch(resource: URI, opts: WatchOptions): Disposable;
    stat(resource: URI): Promise<Stat>;
    readFile(resource: URI): Promise<Uint8Array>;
    /**
     * Maps the URI back to the path the build actually wrote, built through `Endpoint` so this
     * still works when the application is deployed under a sub-path. `URI.path` is decoded, so it
     * has to be re-encoded - otherwise a `#` or `?` in an asset name would be read as a fragment or
     * query.
     */
    protected toUrl(resource: URI): string;
    protected fetch(resource: URI, method: 'GET' | 'HEAD'): Promise<Response>;
    readdir(resource: URI): Promise<[string, FileType][]>;
    mkdir(resource: URI): Promise<void>;
    delete(resource: URI, opts: FileDeleteOptions): Promise<void>;
    rename(from: URI, to: URI, opts: FileOverwriteOptions): Promise<void>;
    writeFile(resource: URI, content: Uint8Array, opts: FileWriteOptions): Promise<void>;
}
export declare class BrowserOnlyPluginFileServiceContribution implements FileServiceContribution {
    protected readonly provider: BrowserOnlyPluginFileSystemProvider;
    registerFileSystemProviders(service: FileService): void;
}
//# sourceMappingURL=browser-only-plugin-file-system-provider.d.ts.map