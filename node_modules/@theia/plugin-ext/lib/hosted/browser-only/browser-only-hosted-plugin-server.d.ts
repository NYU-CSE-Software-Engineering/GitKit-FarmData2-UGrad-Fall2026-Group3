import { DeployedPlugin, ExtPluginApi, HostedPluginClient, HostedPluginServer, PluginIdentifiers } from '../../common';
import { Event, RpcConnectionEventEmitter } from '@theia/core';
export declare const BrowserOnlyPluginOptions: unique symbol;
/**
 * Optional override for the statically deployed plugins of a browser-only application. By
 * default we use whatever the build prepared, i.e. `lib/frontend/hostedPlugin/list.json`. Bind
 * this to supply the metadata by hand instead, e.g. if the plugins are hosted somewhere the
 * build can't see.
 */
export interface BrowserOnlyPluginOptions {
    pluginMetadata: DeployedPlugin[];
}
/**
 * Serves the plugins statically deployed into the frontend bundle. There's no backend in a
 * browser-only application, so nothing can be deployed or undeployed at runtime.
 */
export declare class BrowserOnlyHostedPluginServer implements HostedPluginServer, RpcConnectionEventEmitter {
    readonly onDidOpenConnection: Event<void>;
    readonly onDidCloseConnection: Event<void>;
    protected readonly options?: BrowserOnlyPluginOptions;
    protected client: HostedPluginClient | undefined;
    protected plugins: Promise<DeployedPlugin[]> | undefined;
    /** The statically deployed plugins, from {@link BrowserOnlyPluginOptions} if bound, otherwise from the list the build wrote. */
    protected getPlugins(): Promise<DeployedPlugin[]>;
    protected fetchDeployedPlugins(): Promise<DeployedPlugin[]>;
    getDeployedPluginIds(): Promise<PluginIdentifiers.VersionedId[]>;
    getInstalledPluginIds(): Promise<PluginIdentifiers.VersionedId[]>;
    getUninstalledPluginIds(): Promise<readonly PluginIdentifiers.VersionedId[]>;
    getDisabledPluginIds(): Promise<readonly PluginIdentifiers.UnversionedId[]>;
    getDeployedPlugins(ids: PluginIdentifiers.VersionedId[]): Promise<DeployedPlugin[]>;
    getExtPluginAPI(): Promise<ExtPluginApi[]>;
    onMessage(targetHost: string, message: Uint8Array): Promise<void>;
    setClient(client: HostedPluginClient | undefined): void;
    getClient(): HostedPluginClient | undefined;
    dispose(): void;
}
//# sourceMappingURL=browser-only-hosted-plugin-server.d.ts.map