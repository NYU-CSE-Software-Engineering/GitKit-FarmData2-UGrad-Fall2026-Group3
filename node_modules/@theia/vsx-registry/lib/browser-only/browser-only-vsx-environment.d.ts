import URI from '@theia/core/lib/common/uri';
import { OVSXRouterConfig } from '@theia/ovsx-client';
import { VSXEnvironment } from '../common/vsx-environment';
/**
 * Same defaults as `VSXEnvironmentImpl` when nothing is configured. There's no backend to ask in a
 * browser-only app, and `VSXExtension` looks up the registry URI eagerly, so without this every
 * extension logs a failed RPC call. Rebind it to use a different registry.
 */
export declare class BrowserOnlyVSXEnvironment implements VSXEnvironment {
    protected readonly registryUri: URI;
    getRateLimit(): Promise<number>;
    getRegistryUri(): Promise<string>;
    getRegistryApiUri(): Promise<string>;
    getVscodeApiVersion(): Promise<string>;
    getOvsxRouterConfig(): Promise<OVSXRouterConfig | undefined>;
}
//# sourceMappingURL=browser-only-vsx-environment.d.ts.map